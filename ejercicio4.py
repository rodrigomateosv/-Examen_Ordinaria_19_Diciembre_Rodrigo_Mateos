


def do_math(cadena)
    numeros=cadena.split()
    numeros11=numeros[]  
    letras=[]
    for numero in numeros:
      numeros11.append(numero[:-1])
      letras.append(numero[:1]])
        
    numeros11.sort()
     numeros= []
    for i in range(len(numeros11)):   
      numeros.append(numeros11[i]+letras[i])

      cadena= for numero in numeros:
        cadena=cadena+numero+" "
        
    return cadena
